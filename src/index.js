import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  // <React.StrictMode> removed because of constant errors by material-ui.
  <App>
  </App>,
  document.getElementById('root')
);
